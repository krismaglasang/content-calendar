spring.application.name=content-calendar

spring.config.import=optional:file:.env[.properties]

spring.sql.init.mode=always
#spring.sql.init.schema-locations=

spring.datasource.url=jdbc:postgresql://localhost:${PG_PORT}/${PG_DB}
spring.datasource.username=${PG_USERNAME}
spring.datasource.password=${PG_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Section to demonstrate the use of Configuration Properties
postgres.username=krismaglasang
postgres.password=ssshhh